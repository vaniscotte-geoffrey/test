import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'devis-poc',
  webDir: 'dist/devis-poc/browser'
};

export default config;
